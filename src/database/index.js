import { SCHEMAS as ContentSchemas } from '../modules/Content/database';

// Register Schemas here
export const SCHEMAS = [...ContentSchemas];

// Increment this for every db structure change
export const SCHEMA_VERSION = 0;
